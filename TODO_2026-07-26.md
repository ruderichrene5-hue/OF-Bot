# To-Do — 26 Jul 2026

Goal for today: get the "Run from Airtable" button working end-to-end against the **test base**, and keep hardening the UIAutomator2 flows.

## A. UIAutomator2 flows (tests + improvements)
- [x] **IG opened on the Reels tab → switch to home feed** — shared `_ensure_instagram_home_feed_u2` (taps Home when the Home tab isn't the selected bottom-nav tab; theme-independent, unlike a black/white screen check). Wired into the u2 reel flow at startup. *(To do: add the same call to the other u2 flows — bio/warmup/scroll — and test live.)*
- [x] **Recover from the "Edits" Play Store deep-link** — `_recover_to_instagram_u2` (u2 reel flow) presses Back / re-fronts IG when the foreground app isn't Instagram, so the Google Play "Edits" install page no longer quits the flow. *(To do: port to dump + story flows.)*
- [ ] Tune `update_bio_u2` selectors on a live device and confirm it runs start-to-finish.
- [ ] Run the existing u2 flows on a real phone; note which steps fail / are flaky.
- [ ] Firm up reel upload as a u2 flow (`instagram_reel_upload_u2`) for reliability.
- [ ] Handle animating screens (dumpsys/OCR fallback) + Instagram pop-ups/interruptions mid-flow.

## B. Airtable orchestrator (get it testable ASAP)
- [x] Point the app config at the **test base** (`appNAm6iTuzmOn4ib`) — default base id in `settings.py` (saved value / env still win). *(Token still entered in Dev controls.)*
- [x] Replace the old flat `Ready`/`Flow` reader with a planner (`airtable_planner.plan_airtable_runs`) that reads **Accounts** + linked Profile. Legacy flat client/helpers kept so existing tests stay green.
- [x] Use the **MLX API ID** (18-digit) as the launch key, not the serial.
- [x] Pick each account's due flow(s) via `lifecycle.plan_actions_for_day(Creation Date, today)`.
- [x] Skip guards: `Automation Mode = Paused`, `Lifecycle Stage = Paused/Banned`, `Needs Human Verification`.
- [x] Write results back: create a **Run Log** row (Running→Done/Skipped/Failed) + update `Last Run`/`Last Result`; human-verification also sets the account's Needs-Verification checkbox.
- [x] Idempotency: skip a flow already logged Done/Running for that account today.
- [x] **UI run summary** — a "Last run summary" panel lists Success / Failed / Needs-verification / Skipped profiles after each run (`_build_run_summary` in `ui.py`). Also mirrored to the log.
- [x] **Run selected from Airtable** — checkbox-selected profiles restrict the run (none selected = all); after the button click a confirm dialog reports how many profiles will run (and how many were skipped by guards) before launching (`selected_launch_ids` in planner/runner + `_confirm_airtable_run` in `ui.py`).
- [x] **Flow source toggle ("Use UI flow")** — a checkbox next to Run from Airtable: OFF = each profile runs its lifecycle-due flow; ON = every in-scope profile runs the UI-selected flow with the UI's caption/bio/picture (bypasses creation-date/idempotency/reel gating, keeps paused/banned/needs-verification guards). Wired via `override_flow` etc. in planner/runner; confirm dialog names the flow.
- [ ] **Reels stay off until media is wired** (`run_reels=False` in the planner) — flip on once the media source is decided. Note: the Nikki test accounts have June/early-July creation dates → they're in the **posting phase**, so a live run today finds "nothing due" unless you (a) set an account's Creation Date to within the last ~5 days to exercise warmup/bio, or (b) enable reels.

## C. First end-to-end test
- [x] **DONE (27 Jul):** Ran from Airtable on the "Rodrigo (test)" account — override `update_bio_u2` (Skipped: already had a bio) and lifecycle day-3 (warmup **Done**, picture/bio failed for missing inputs). Full loop verified: plan → launch → ADB connect → u2 flow → Run Log + Last Run/Result write-back → UI summary.
- [x] **Pre-flight input check** — flows with a missing required input (bio text / picture) now log a **Skipped** Run Log row with a clear reason instead of launching + failing blank; genuine failures now carry a note too (`_missing_input_reason` in `airtable_runner.py`).

## C2. Content sources so bio/picture/reels can actually succeed
- [ ] **Per-account bio** — populate `Accounts.Bio` in the base so lifecycle `update_bio` has text (empty today).
- [ ] **Profile-picture source** — download `Accounts.Profile Picture` attachment and hand it to the picture flow (unwired → always skips in lifecycle mode).
- [ ] **Reel media source** — decide local vs Google Drive, then flip `run_reels=True`.
- [ ] **Onboard more Accounts** — only ~11 accounts exist (Nikki 1–10 + Rodrigo test); add rows for the other profiles you want to run from Airtable.

## D. Reel upload flow — reliability (do one at a time, test on a live device after each)
- [x] **REEL tab off-screen on small screens** — swipe the mode carousel right→left to reveal REEL, only when it isn't already visible (done in `_select_reel_mode_u2` / `_reveal_reel_option_center`).
- [x] **Confirm REEL mode is actually entered** before selecting media — u2 flow now taps REEL, confirms the tab's `selected` state (re-tapping if not), and refuses to pick media if a non-REEL mode stays selected (`_select_reel_mode_u2` / `_tab_is_selected_u2`). *(Test on live device; then optionally mirror the selected-state guard to the dump flow.)*
- [~] **"Edits" video-editor promo when posting** — two variants: (a) in-app promo covering Next → `_dismiss_edit_app_popup_u2` (dismiss-control → Back → tap-outside); (b) it deep-links to the **Google Play Store install page**, taking IG out of the foreground → NEW `_recover_to_instagram_u2` (checks `app_current()`; if not IG, presses Back to close Play Store / brings IG to front without restart; never force-stops IG). Wired into the popup-dismiss + before the Share tap in the u2 flow. To do: confirm both live, and port to the dump + story flows.
- [x] **Confirm the post before declaring success (both flows)** — after Share, keep Instagram open and poll the phone for the "posted" confirmation banner for up to **5 min**; `success` is now returned only when confirmed (dump: `_adb_wait_for_reel_post_confirmation`; u2 wait raised 45s→300s and gates success). Phrase list tightened to reel-specific/celebratory to avoid feed false-positives. *(Test live: confirm the phrase list matches what your IG build shows on a real post.)*
- [ ] **Verify the screen advanced after Next** (caption/share screen appeared) before continuing, instead of trusting the blind ratio fallback taps.
- [ ] **Smarter media-cell pick** — prefer the just-pushed video file / an explicit video cell instead of a blind first-thumbnail tap.
- [ ] **Dismiss leftover draft dialogs on launch** ("Discard / Keep draft" from a previous aborted run can block the next run).
- [ ] **Log "no media" as Skipped** in the Run Log instead of a silent `success:False`.
- [ ] **Mirror the REEL-reveal swipe fix into the story flow** (same bottom-carousel selector).

_Deferred (not today): reels media source (local vs Google Drive), `update_profile_picture` flow, MLX profile tags driving flow selection._
